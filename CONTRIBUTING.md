# Contributing

Fork this repository and open a pull request against **`dev`** — not `main`.

Only [pickpoint](https://github.com/pickpoint) organization members can merge `dev` or `main`. Releases are cut by merging `dev` into `main`.

See the README for local checks, CI, and `[skip release]`.
